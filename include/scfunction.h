#ifndef SCFUNCTION_H
#define SCFUNCTION_H

#include "scinexpression.h"
#include "scparsedexpression.h"

/*
class SCFunction
{
public:
    SCFunction();
    SCFunction(const SCInExpression &inExp, const SCParsedExpression &parsedExp, const QStringList &params);

    QStringList getTokens() const;
    QStringList getParsedTokens() const;
    QStringList getParameters() const;

private:
    SCInExpression expression;
    SCParsedExpression parsedExpression;
    QStringList parameters;
};
*/
#endif // SCFUNCTION_H
