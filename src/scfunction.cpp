#include "include/scfunction.h"

/*
SCFunction::SCFunction() {}


SCFunction::SCFunction(const SCInExpression &inExp, const SCParsedExpression &parsedExp, const QStringList &params):
    expression(inExp),
    parsedExpression(parsedExp),
    parameters(params)
{}

QStringList SCFunction::getTokens() const
{
    return expression.getTokens();
}

QStringList SCFunction::getParsedTokens() const
{
    return parsedExpression.getTokens();
}

QStringList SCFunction::getParameters() const
{
    return parameters;
}
*/
